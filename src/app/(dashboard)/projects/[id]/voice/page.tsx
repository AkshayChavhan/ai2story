/**
 * Voice & Audio Setup — StoryForge AI
 * Will be fully implemented in feat:7_voice-generation.
 */
export default function VoicePage() {
  return (
    <div className="p-8">
      <h1 className="text-3xl font-bold text-foreground">Voice & Audio</h1>
      <p className="mt-2 text-muted-foreground">
        Select voices and generate voiceovers for each scene.
      </p>
    </div>
  );
}
